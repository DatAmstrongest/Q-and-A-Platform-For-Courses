# redis-config.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
  namespace: default
data:
  redis.conf: |
    maxmemory 5mb
    maxmemory-policy allkeys-lru
    save ""
    appendonly no